extends _base.pug

block title
	+title('Version')

block main 
	main
		h3.version #{package.version}
		p.description #{package.description}
		p.repository #{package.repository}
		p.license #{package.license}
		p.keywords #{package.keywords}
		p.author #{package.author}
		p.homepage #{package.homepage}
